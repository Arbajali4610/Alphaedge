services:
  - type: web
    name: alphaedge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
    envVars:
      - key: MARKET_UPSTREAM
        value: http://65.0.104.9/stock/list
