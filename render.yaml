services:
  - type: web
    name: alphaedge-live
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: UPSTOX_ACCESS_TOKEN
        sync: false
